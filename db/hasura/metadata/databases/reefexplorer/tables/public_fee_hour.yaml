table:
  schema: public
  name: fee_hour
object_relationships:
  - name: pool
    using:
      foreign_key_constraint_on: pool_id
select_permissions:
  - role: public
  permission:
    columns:
      - pool_id
      - fee
      - timeframe
    filter: {}
    allow_aggregations: true